# Secrets (GCP service-account keys have unpredictable names; ignore all JSON)
.env
credentials.json
*.json

# Local data (ChromaDB / offline store, temp files)
data/
*.log

# Python
__pycache__/
*.py[cod]
venv/
.venv/
*.egg-info/
.pytest_cache/
