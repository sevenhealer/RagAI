"""Offline, fully-local RAG stack: Ollama (LLM + embeddings) + ChromaDB.

This subsystem requires no authentication and no cloud services. It mirrors the
online streaming/citation contract so the frontend can treat both identically.
"""
